import React from 'react';
import ProfileInfo from './profileInfo';

const OrgAccountInfo = ({ profileData }) => {
  return (
    <>
      <ProfileInfo profileData={profileData} />
    </>
  );
};

export default OrgAccountInfo;
